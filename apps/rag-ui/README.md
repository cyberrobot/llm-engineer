# RAG UI

The React web application for AI Discovery Assistant. It is managed as an npm workspace from the repository root.

```sh
npm ci
npm run dev
```

Set `VITE_API_URL` in `apps/rag-ui/.env` to the API URL.

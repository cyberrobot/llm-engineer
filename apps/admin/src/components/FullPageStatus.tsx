export function FullPageStatus({ title, message, action }: { title:string; message:string; action?:{label:string;onClick():void} }) { return <main className="status-page"><section className="status-card" aria-live="polite"><div className="brand-mark" aria-hidden="true">R</div><h1>{title}</h1><p>{message}</p>{action&&<button onClick={action.onClick}>{action.label}</button>}</section></main> }
export function ConfigurationError({ variable, reason }: { variable:string; reason:'missing'|'invalid' }) { return <FullPageStatus title="Admin configuration required" message={`${variable} is ${reason}. Set it to an absolute HTTP or HTTPS backend URL and restart the application.`}/> }

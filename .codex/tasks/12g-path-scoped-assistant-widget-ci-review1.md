PR 12G Review 1 — Path-Scoped Assistant Widget CI Verification

Repository state

Expected branch:

12g-path-scoped-assistant-widget-ci

Base branch:

main

Worktree:

Frontend

Dependencies:

- PR #84 — Scope Assistant widget CI checks to widget-relevant changes
- .codex/tasks/12g-path-scoped-assistant-widget-ci.md
- Existing dedicated .github/workflows/test-assistant-widget.yml
- Existing generic .github/workflows/test.yml
- Existing Changesets configuration
- Existing @redmoor/assistant-widget package
- Existing widget publishing workflow

This is a review-fix task for PR #84.

Do not create a new branch.

Implement the required repository changes on:

12g-path-scoped-assistant-widget-ci

Read first

- AGENTS.md
- docs/architecture/repository-map.md
- docs/architecture/dependency-rules.md
- .codex/tasks/12g-path-scoped-assistant-widget-ci.md
- .github/workflows/test.yml
- .github/workflows/test-assistant-widget.yml
- .github/workflows/publish-assistant-widget.yml
- .changeset/config.json
- package.json
- package-lock.json
- packages/assistant-widget/package.json
- packages/assistant-widget/README.md

Inspect the current PR #84 branch state before making changes.

Primary change area

- .github/workflows/test-assistant-widget.yml
- .github/workflows/test.yml
- packages/assistant-widget/README.md
- Existing CI validation/documentation files where required
- Focused workflow-validation tests or scripts only where repository conventions already support them

Canonical implementation examples

Preserve the architecture introduced by PR #84:

Generic Tests workflow
├── Backend tests
└── Storybook tests
Dedicated Assistant widget workflow
└── path-scoped pull_request trigger
└── full widget validation pipeline

Use:

- GitHub Actions native pull_request.paths
- actions/checkout@v4
- actions/setup-node@v4
- @changesets/cli
- existing npm workspace scripts

Do not reintroduce:

- dorny/paths-filter
- path detector jobs
- no-op widget jobs
- fake successful widget statuses
- custom semantic-version logic

Relevant symbols

- Assistant widget
- .github/workflows/test-assistant-widget.yml
- .github/workflows/test.yml
- pull_request.paths
- push.branches
- changeset-release/main
- github.event.pull_request.base.sha
- npx changeset status
- @redmoor/assistant-widget
- npm run lint --workspace
- npm run test --workspace
- npm run build --workspace
- npm run pack:verify --workspace

Expected change surface

This task is narrowly scoped to completing the repository-side requirements identified during review of PR #84.

Expected work includes:

- validating the new dedicated widget workflow configuration;
- ensuring all required positive-path widget checks actually execute;
- ensuring workflow naming and status semantics are unambiguous;
- adding repository-level verification where practical;
- correcting documentation where it currently overstates unverified behaviour;
- preserving all PR 12G functionality already implemented.

Excluded areas

Do not:

- recreate PR 12G;
- move widget CI back into the generic Tests workflow;
- reintroduce runtime path detection;
- add placeholder/no-op jobs;
- modify widget runtime behaviour;
- modify Admin or backend production code;
- modify package exports;
- change Changesets release semantics;
- change npm publishing semantics;
- add custom release orchestration;
- weaken lint, tests, build or package verification;
- add unrelated task specifications;
- change unrelated CI jobs.

Unknowns Codex must verify

Before implementation, verify:

- the exact workflow/job check name generated by .github/workflows/test-assistant-widget.yml;
- whether the current workflow syntax is valid and accepted by GitHub Actions;
- whether all widget validation steps execute on a PR changing .github/workflows/test-assistant-widget.yml;
- whether the same-repository changeset-release/main exception remains correctly scoped;
- whether fork PRs remain unable to spoof the Changesets exception;
- whether pull_request.paths includes every repository file that can materially affect the widget package;
- whether any shared configuration outside the current path list is inherited by the widget;
- whether the current README claims behaviour that cannot yet be proven from repository-level evidence;
- whether an existing repository validation utility can exercise workflow path selection or YAML correctness without adding bespoke infrastructure.

Do not make speculative dependency-path additions. Add only verified widget-affecting inputs.

⸻

Objective

Complete PR #84 so the dedicated Assistant widget workflow is fully verified, maintainable and consistent with PR 12G.

The desired CI semantics remain:

Unrelated PR
→ no Assistant widget workflow
Widget-relevant PR
→ Assistant widget workflow appears
→ full package validation actually runs

The repository must make it clear that a visible Assistant widget status means:

- checkout executed;
- dependencies installed;
- applicable Changesets validation ran;
- lint ran;
- tests ran;
- build ran;
- package verification ran.

No repository-side implementation may create a successful widget status without performing those checks.

Current architecture

PR #84 introduced:

.github/workflows/test-assistant-widget.yml

with path-scoped pull-request triggers for:

- packages/assistant-widget/\*\*
- .changeset/\*\*
- package.json
- package-lock.json
- .github/workflows/test-assistant-widget.yml
- .github/workflows/publish-assistant-widget.yml

The workflow runs the full widget validation pipeline when triggered.

PR #84 also removes the previous assistant-widget job from:

.github/workflows/test.yml

The architectural direction is correct.

The remaining review gap is not the high-level workflow structure. It is incomplete repository-side verification and evidence that the workflow behaves as intended.

Required implementation

1. Preserve the dedicated workflow architecture

Keep Assistant widget pull-request validation in:

.github/workflows/test-assistant-widget.yml

Keep the generic:

.github/workflows/test.yml

limited to its non-widget jobs.

Do not reintroduce:

assistant-widget

into the generic workflow.

2. Preserve workflow-level path filtering

Keep pull_request.paths as the relevance boundary.

The widget workflow must not use an internal path-detection step.

The current minimum path set must remain unless repository inspection proves additions are required:

packages/assistant-widget/**
.changeset/**
package.json
package-lock.json
.github/workflows/test-assistant-widget.yml
.github/workflows/publish-assistant-widget.yml

If another shared configuration file directly affects widget compilation, linting, testing or packaging, add it and document the dependency.

Do not broaden the path set to unrelated application/backend/documentation directories.

3. Verify workflow YAML structurally

Add or use existing repository-level validation to ensure:

- both workflow files parse as valid YAML;
- the dedicated workflow contains a pull_request.paths filter;
- the generic workflow does not contain an Assistant widget job;
- the dedicated workflow contains the required widget validation commands.

Prefer existing project tooling.

Do not introduce a large bespoke workflow testing framework.

4. Verify the full widget pipeline remains unconditional when triggered

Inside the dedicated workflow, the following steps must remain part of every triggered widget validation job:

checkout
setup Node
npm ci
lint
test
build
pack verification

Changesets validation may remain conditionally skipped only for the canonical same-repository Release PR.

Do not add conditions that allow a relevant widget PR to produce a successful widget status while skipping one of the package quality gates.

5. Preserve full-history checkout

Keep:

fetch-depth: 0

for the widget workflow.

Changesets must be able to compare against the exact PR base commit.

6. Preserve exact Changesets base comparison

For ordinary widget-affecting pull requests, preserve:

npx changeset status --since=${{ github.event.pull_request.base.sha }}

Do not replace the exact base SHA with an ambiguous moving branch reference.

7. Preserve the Release PR exception safely

The Changesets check may be skipped only when both are true:

- the PR head repository is the same repository;
- the PR head branch is changeset-release/main.

A fork using:

changeset-release/main

must still run the Changesets check.

Do not weaken this logic.

8. Preserve push-to-main widget verification

Keep the widget workflow configured for pushes to:

main

unless repository evidence proves another existing release workflow provides strictly equivalent protection and the governing PR 12G spec permits removal.

Do not remove push validation solely to reduce CI execution.

9. Make workflow/check naming unambiguous

Ensure GitHub displays a widget-specific check whose name clearly represents actual validation.

Avoid duplicate names that make it unclear whether a status came from:

- the generic Tests workflow;
- the dedicated Assistant widget workflow;
- another release workflow.

Prefer one clearly identifiable widget validation status.

If necessary, adjust the workflow or job name so the resulting GitHub check is unambiguous while remaining concise.

10. Correct documentation to match repository guarantees

Update:

packages/assistant-widget/README.md

only where necessary so it distinguishes:

- repository-controlled workflow behaviour;
- repository configuration expectations outside source control.

The README may state that the workflow is path-scoped and does not run for unrelated paths.

Do not present external repository/ruleset configuration as something guaranteed by the codebase itself.

Keep the documentation factual and verifiable from repository state.

11. Add focused regression validation for workflow structure

Where repository conventions support it, add focused tests or validation covering:

Generic workflow

Assert that:

.github/workflows/test.yml

does not define:

assistant-widget

Dedicated workflow

Assert that:

.github/workflows/test-assistant-widget.yml

includes:

- pull_request;
- paths;
- push to main;
- checkout;
- full history;
- Node setup;
- npm ci;
- Changesets validation;
- lint;
- tests;
- build;
- pack:verify.

Do not test implementation details of GitHub Actions itself.

12. Validate the trigger matrix statically

Document or test the expected path-selection matrix using repository-level validation where practical:

packages/assistant-widget/** triggers
package.json triggers
package-lock.json triggers
.changeset/** triggers
test-assistant-widget workflow triggers
publish-assistant-widget workflow triggers
apps/admin/** only does not trigger
apps/backend/** only does not trigger
docs/** only does not trigger
.codex/tasks/** only does not trigger

Do not add runtime path filtering to accomplish this.

13. Remove stale PR 12F no-op wording

Search repository documentation for wording that describes the previous model where:

Assistant widget

was always present but package steps were skipped.

Remove or update that wording.

The repository must consistently describe the PR 12G model:

check present = full widget validation ran
check absent = widget workflow was not triggered

Acceptance criteria

- .github/workflows/test-assistant-widget.yml exists.
- .github/workflows/test.yml contains no Assistant widget job.
- No dorny/paths-filter widget relevance logic remains in PR CI.
- No standalone widget path-detection job exists.
- No no-op Assistant widget job exists.
- The dedicated workflow uses pull_request.paths.
- Widget source changes are included in the path filter.
- Root npm manifest changes are included in the path filter.
- Lockfile changes are included in the path filter.
- Changesets changes are included in the path filter.
- Widget workflow changes are included in the path filter.
- Widget publishing workflow changes are included in the path filter.
- Unrelated Admin paths are excluded.
- Unrelated backend paths are excluded.
- Unrelated docs/task paths are excluded.
- Checkout uses full Git history.
- npm ci runs on every triggered widget validation.
- Widget lint runs on every triggered widget validation.
- Widget tests run on every triggered widget validation.
- Widget build runs on every triggered widget validation.
- pack:verify runs on every triggered widget validation.
- Ordinary widget PRs run changeset status against the exact PR base SHA.
- The same-repository changeset-release/main exception remains intact.
- Fork PRs cannot spoof the Release PR exception.
- Pushes to main retain widget quality-gate execution.
- Workflow/check naming clearly represents actual widget validation.
- Repository documentation no longer describes the PR 12F no-op-check model.
- Modified workflow YAML parses successfully.
- git diff --check passes.
- Existing widget lint/tests/build/package verification pass.
- No unrelated production code changes are introduced.

Tests to add or update

Add focused workflow/configuration validation where existing repository conventions permit.

Important cases:

Relevant widget source

packages/assistant-widget/src/\*\*

Expected:

test-assistant-widget.yml path configuration includes the change.

Root dependency graph

package.json
package-lock.json

Expected:

widget workflow is configured to trigger.

Changesets

.changeset/\*\*

Expected:

widget workflow is configured to trigger.

Workflow infrastructure

.github/workflows/test-assistant-widget.yml
.github/workflows/publish-assistant-widget.yml

Expected:

widget workflow is configured to trigger.

Admin-only changes

apps/admin/\*\*

Expected:

no matching widget workflow path.

Backend-only changes

apps/backend/\*\*

Expected:

no matching widget workflow path unless a specifically verified shared dependency is introduced.

Documentation/task-only changes

docs/**
.codex/tasks/**

Expected:

no matching widget workflow path.

Release PR exception

Verify the workflow expression exempts only:

same repository + changeset-release/main

and not a fork with the same branch name.

Verification commands

git diff --check
npm ci
npm run lint --workspace @redmoor/assistant-widget
npm run test --workspace @redmoor/assistant-widget
npm run build --workspace @redmoor/assistant-widget
npm run pack:verify --workspace @redmoor/assistant-widget
npx changeset status

Validate modified workflow YAML using an existing parser available in the repository or development environment.

Inspect:

.github/workflows/test.yml

and confirm:

Backend tests
Storybook tests
No Assistant widget job

Inspect:

.github/workflows/test-assistant-widget.yml

and confirm:

path-scoped pull_request trigger
push to main
checkout with full history
npm ci
Changesets validation
lint
tests
build
pack verification

Do not report this review-fix task complete until all repository-side verification commands pass and the final diff contains no reintroduction of no-op widget status logic.
